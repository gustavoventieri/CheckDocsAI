FROM python:3.11-slim
WORKDIR /app

# Copiar requirements.txt (se tiver)
COPY rag-server/requirements.txt ./
RUN pip install --no-cache-dir -r requirements.txt

# Copiar código do RAG Server
COPY rag-server/ .

EXPOSE 5000
CMD ["python", "app.py"]
